# Package Manifest Filenames

Files at the project root that declare dependencies and identify the project's primary language.

| Filename | Language |
|----------|----------|
| `package.json` | JavaScript / TypeScript |
| `pyproject.toml` | Python |
| `Cargo.toml` | Rust |
| `go.mod` | Go |
